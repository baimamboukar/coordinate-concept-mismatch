extends: olmo1_independent_training_boolq.yaml
name: olmo1_map_generalization_sst2_to_boolq

pipeline:
  stages:
    align:
      name: olmo1_heldout_map_generalization
      artifact_variant: sst2-fit-boolq-eval
      fit_materials:
        expected_train_rows: 6000
        task_balanced: false
        datasets:
          - dataset_key: sst2-sentiment-v1
            source_study: olmo1_independent_training_sst2
            expected_train_rows: 12000
            fit_rows: 6000
      reference_materials:
        source_name: olmo1_independent_alignment
        source_study: olmo1_independent_training_boolq
        source_variant: boolq-alignment
